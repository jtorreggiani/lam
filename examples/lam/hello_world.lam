; Example LAM program
; This program sets register R0 to 42, stores a variable in R1,
; checks that R0 contains 42, then calls the built-in "halt" predicate, and finally halts.

PUT_CONST R0, 42
PUT_VAR   R1, 0, "X"
GET_CONST R0, 42
CALL      "print"
CALL      "halt"
HALT
